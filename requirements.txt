streamlit>=1.30
pandas>=1.5
matplotlib>=3.6
streamlit-sortables>=0.3

# Optional — only needed when per-student persistence (student_store) is
# configured with storage secrets. The app runs fully without these.
# cryptography>=41
# dropbox>=11
