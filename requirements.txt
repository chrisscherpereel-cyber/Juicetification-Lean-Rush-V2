streamlit>=1.30
pandas>=1.5
matplotlib>=3.6
streamlit-sortables>=0.3

# Required for per-student persistence (student_store) once storage secrets are
# configured. Harmless if storage is left unconfigured (imported lazily).
cryptography>=41
dropbox>=11
